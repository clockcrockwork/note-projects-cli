{
  "name": "note-projects-cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "test": "node --test",
    "check": "node --check scripts/*.mjs && npm test"
  }
}
